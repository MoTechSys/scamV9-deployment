# S-ACM - نظام إدارة المحتوى الأكاديمي الذكي

<div dir="rtl">

## 🎯 نظرة عامة

**S-ACM (Smart Academic Content Management)** هو نظام ويب متكامل لإدارة المحتوى الأكاديمي، مصمم لتسهيل توزيع المواد الدراسية وتعزيز التواصل بين المدرسين والطلاب، مع دمج ميزات الذكاء الاصطناعي المتقدمة.

## ✨ الميزات الرئيسية

### للطلاب
- 📚 الوصول السريع والمنظم للمواد الدراسية
- 🤖 تلخيص المحاضرات بالذكاء الاصطناعي
- ❓ توليد أسئلة تفاعلية من المحتوى
- 💬 ميزة "اسأل المستند" للحصول على إجابات فورية
- 📁 أرشيف المواد السابقة

### للمدرسين
- 📤 رفع وتنظيم المحتوى بسهولة
- 📢 إرسال إشعارات للطلاب
- 📊 متابعة إحصائيات التفاعل
- 👁️ التحكم في ظهور الملفات

### للمسؤولين
- 👥 إدارة المستخدمين (استيراد/تصدير CSV)
- 🎓 إدارة التخصصات والمستويات
- 📖 إدارة المقررات وتعيين المدرسين
- ⬆️ ترقية الطلاب الجماعية
- 📈 لوحة تحكم شاملة

## 🛠️ المكدس التقني

| المكون | التقنية |
|--------|---------|
| Backend | Django 5.x |
| Database | PostgreSQL / SQLite |
| Frontend | Bootstrap 5 + HTMX |
| AI | Google Gemini API |
| Auth | Django Auth + RBAC |

## 📦 التثبيت

### المتطلبات
- Python 3.11+
- PostgreSQL 15+ (للإنتاج) أو SQLite (للتطوير)

### خطوات التثبيت

```bash
# استنساخ المستودع
git clone https://github.com/MoainAlabbasi/SCAMpro.git
cd SCAMpro

# إنشاء البيئة الافتراضية
python -m venv venv

# تفعيل البيئة (Windows)
venv\Scripts\activate

# تفعيل البيئة (Linux/Mac)
source venv/bin/activate

# تثبيت المتطلبات
pip install -r requirements.txt

# إعداد ملف البيئة
cp .env.example .env
# قم بتعديل .env بإعداداتك

# تطبيق migrations
python manage.py migrate

# إنشاء مستخدم مسؤول
python manage.py createsuperuser

# تشغيل السيرفر
python manage.py runserver
```

## 📁 هيكل المشروع

```
ProjectSCAM/
├── apps/                # التطبيقات الرئيسية
│   ├── accounts/        # إدارة المستخدمين والأدوار
│   ├── ai_features/    # ميزات الذكاء الاصطناعي
│   ├── core/           # الوظائف الأساسية
│   ├── courses/        # إدارة المقررات والملفات
│   └── notifications/  # نظام الإشعارات
├── config/              # إعدادات Django
├── templates/           # قوالب HTML
├── static/              # الملفات الثابتة
├── media/               # الملفات المرفوعة
├── logs/                # ملفات السجلات
└── requirements.txt     # المتطلبات
```

## 🔐 نظام الصلاحيات (RBAC)

| الدور | الصلاحيات |
|-------|-----------|
| Admin | تحكم كامل في النظام |
| Instructor | إدارة مقرراته، رفع الملفات، إرسال إشعارات |
| Student | تصفح المواد، استخدام AI، تلقي إشعارات |

## 🤖 ميزات الذكاء الاصطناعي

### محرك AI Enterprise v2 (Dynamic Governance)

النظام يحتوي على محرك ذكاء اصطناعي متقدم مع تحكم كامل من لوحة الأدمن:

- **التلخيص**: تلخيص المحاضرات الطويلة تلقائياً مع Smart Chunking
- **توليد الأسئلة**: إنشاء أسئلة اختبارية من المحتوى (MCQ, صح/خطأ, إجابة قصيرة)
- **اسأل المستند**: طرح أسئلة والحصول على إجابات من سياق الملف
- **حد الاستخدام**: قابل للتعديل من لوحة التحكم (افتراضي: 10 طلبات/ساعة)

### إدارة AI من لوحة الأدمن (بدون لمس الكود)

| الميزة | الوصف |
|--------|-------|
| **AIConfiguration** | Singleton للتحكم في النموذج النشط، حجم التقطيع، التوكنات، درجة الإبداع |
| **مفاتيح API مشفرة** | تخزين مشفر مع XOR + Base64، تدوير تلقائي Round-Robin |
| **Hydra Key Manager** | تدوير ذكي بين المفاتيح مع Cooldown تلقائي عند Rate Limit |
| **اختبار الاتصال** | زر "Test Connection" لكل مفتاح من لوحة الأدمن |
| **لوحة مراقبة AI** | صحة المفاتيح، التوكنات المستخدمة، معدل الفشل |
| **RPM Throttling** | حد الطلبات/دقيقة لكل مفتاح حسب خطة Google Cloud |
| **تعطيل/تفعيل الخدمة** | إيقاف AI بالكامل مع رسالة صيانة مخصصة |

### تحسينات الأداء (Sub-100ms Target)

| المكون | قبل | بعد |
|--------|------|------|
| InstructorDashboard | Python loops O(n) + memory bloat | DB Aggregate (single SQL) |
| StudentDashboard | N+1 queries (1+3N) | 2 queries via annotate+prefetch |
| LectureFile | 3 indexes | 7 indexes (composite) |
| SmartChunker | Hardcoded 8000 chars | DB-configured (30000 default) |

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT.

## 👥 المساهمون

- **معين العباسي** - مالك المشروع
- **Manus AI** - التطوير والتوثيق

</div>
